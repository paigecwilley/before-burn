import glamorous from 'glamorous';


import * as c from '../constants';
import { spacing } from '../../identity/spacing';

export default glamorous.section(
  spacing({ margin: 'bottom-xl top-xl'}),
  {
    // backgroundColor: 'lightgrey',
  }
)
