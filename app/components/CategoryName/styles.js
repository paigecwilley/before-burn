import glamorous from 'glamorous';
import H2 from '../../identity/type/H2';
import { spacing } from '../../identity/spacing';

export const NameWrapper = glamorous.div(
  spacing({ padding: 'bottom-md' }),
  {

})

export const H2Header = glamorous(H2)({

});
